# Handoff → Antigravity (Gemini)

เราแบ่งงานกันผ่าน git repo นี้ ไม่ได้คุยกันตรง ๆ เอกสารนี้สรุป **สิ่งที่พิสูจน์แล้ว** เพื่อไม่ให้เสียเวลาไล่ทางตันซ้ำ และเสนอส่วนที่เหมาะจะทำต่อ

อัปเดตล่าสุด: commit `9504710`

---

## 1. ข้อสรุปที่พิสูจน์แล้ว — อย่าไล่ซ้ำ

### บริษัทเอกชนไทยไม่มีบอร์ดประกาศจัดซื้อสาธารณะ

รันเครื่องมือค้นหาอัตโนมัติกับ 40 บริษัท (`backend/discover_procurement_pages.py`) ซึ่งอ่าน robots.txt → ลองเส้นทางมาตรฐาน → อ่าน sitemap → พิสูจน์ว่าหน้านั้นมีแถวประกาศที่มีวันที่จริง

| ผลลัพธ์ | จำนวน |
|---|---|
| ไม่มีหน้า procurement เลย | 24 (SCG, PTT, CP, True, AIS, ธนาคารทั้งหมด) |
| **เอกชนที่มีบอร์ดประกาศจริง** | **0 จาก 24** |

เหตุผลเชิงโครงสร้าง: **พ.ร.บ.จัดซื้อจัดจ้างฯ 2560 บังคับเฉพาะหน่วยงานรัฐ** เอกชนใช้ทะเบียนคู่ค้า (AVL) แล้วส่ง RFP ตรงถึงรายที่ลงทะเบียน — ไม่ใช่ปัญหาที่ scraper แก้ได้

**เคสเฉพาะที่ตรวจแล้ว:**
- `scg.com/th/procurement/` → 302 ไปหน้าแรก (หน้าไม่มีอยู่จริง) และ SCG ระบุ `Disallow: /` สำหรับ ClaudeBot/GPTBot/CCBot — source นี้ถูกปิดเป็น `DISABLED_NO_PUBLIC_BOARD` แล้ว
- SET (ตลท.) → Incapsula WAF ตอบ 403 ทุกคำขอที่ไม่ใช่เบราว์เซอร์
- JobsDB → robots ห้าม query string ทั้งหมด และระบุ `anthropic-ai`/`GPTBot`/`CCBot` ไว้ชัด **ห้ามแตะ**
- JobTopGun / JobBKK → robots อนุญาต `/jobs/` อย่างชัดเจน (ถ้าจะทำสัญญาณล่วงหน้า ใช้สองตัวนี้)
- กปน. `li.post` ที่ detector เคยนับได้ = ป้ายปี ไม่ใช่ประกาศ (false positive)
- ไปรษณีย์ไทย = ประกาศจริง แต่เป็น "ประกาศ**ผู้ชนะ**" ตัดสินไปแล้ว

### e-GP ไม่มีวันปิดรับซอง
`project_detail` และ `contract_submit` ตรวจแล้ว — ไม่มี submission deadline สักตัว (`contract_submit` คืนรายชื่อผู้ยื่นและราคา = ปิดรับไปแล้ว) วันยื่นซองมาจากเอกสารเชิญชวนของหน่วยงานเท่านั้น (ONCB, DGA, ETDA, PEA, ธปท.)

---

## 2. สิ่งที่เพิ่งแก้ไป (ฝั่ง backend)

- **ตัดโครงการที่มีผู้ชนะแล้วออกจากมุมมองหลัก** — 191 → 71 รายการ
  `bid_notice_status = "AWARDED"` เซ็ตจากหลักฐานที่เก็บไว้ (`project_detail.contract[].winner.name`)
  ยังเรียกดูได้ด้วย `include_awarded=true` หรือ `opportunity_scope=AWARDED`
  > `opportunity_scope=AWARDED` ที่คุณเพิ่มไว้ ถูกยกเว้นจากตัวกรองนี้แล้ว ไม่งั้นมันจะคืนค่าว่าง
- **มุมมองเริ่มต้น** = ประกาศอายุ ≤ 1 ปี เรียงใหม่→เก่า (`max_age_days`, `0` = ไม่จำกัด) ใช้กับ list + CSV + stats พร้อมกัน
- **`POST /api/scan` ตอบ 202 ทันที** ไม่รอจนจบ, scan ที่ถูกฆ่ากลางคันถูกปิดเป็น `INTERRUPTED` ตอนบูต

---

## 3. เสนอให้ทำต่อ — Supplier Registration Tracker

เมื่อช่องทางจริงของงานเอกชนคือ **การลงทะเบียนเป็นคู่ค้า** ไม่ใช่การ scrape สิ่งที่ช่วยผู้ใช้ได้จริงคือหน้าจัดการว่า "ลงทะเบียนกับใครไปแล้วบ้าง"

งานนี้เป็น frontend + CRUD ซึ่งตรงกับที่คุณทำอยู่ (SourcesModal, live URL test)

**ขอบเขตที่เสนอ**
- ตารางบริษัทเป้าหมาย: ชื่อ, URL หน้าลงทะเบียนคู่ค้า, สถานะ (ยังไม่ลง / ยื่นแล้ว / อนุมัติแล้ว / ถูกปฏิเสธ), วันที่ยื่น, วันที่ต้องต่ออายุ, ผู้ติดต่อ, โน้ต
- เตือนเมื่อใกล้ต้องต่ออายุทะเบียน
- ข้อมูลตั้งต้นใช้ผลจาก `discover_procurement_pages.py --json` ได้ (ไฟล์มี URL ที่ตรวจแล้วว่ามีหน้าคู่ค้าจริง)

**ข้อควรระวังของ repo นี้ (สำคัญ)**
1. **ห้ามปนกับ tender** — ทะเบียนคู่ค้าไม่ใช่ประกาศจัดซื้อ ต้องแยกตาราง/แยกหน้า ไม่งั้นตัวเลขบนการ์ดจะโกหก
2. **ห้ามแต่งข้อมูล** — ฟิลด์ที่ไม่มีต้นทางต้องเป็น `null` ไม่ใช่ค่าเดา (ดู README หัวข้อ "สถานะข้อมูล")
3. **เพิ่ม source ใหม่ต้องพิสูจน์ก่อน** — รัน `discover_procurement_pages.py` หรือยิง URL ดูว่าไม่ redirect ไปหน้าแรก แล้วใส่ `verified_note` บอกว่าเห็นอะไร
4. **เคารพ robots.txt เสมอ** — crawler ตั้ง fail-closed ไว้ ถ้าจะ `robots_fail_open` ต้องมีเหตุผลกำกับ
5. รันเทสต์ก่อน commit: `PYTHONPATH=. backend/venv/bin/python -m unittest discover -s backend/tests` (ตอนนี้ 64 ตัว ผ่านหมด)

---

## 4. ถ้าจะทำสัญญาณล่วงหน้าจากประกาศรับสมัครงาน

ผู้ใช้เคยเลือกแนวนี้ไว้ — บริษัทที่กำลังหาคน SOC/Security คือบริษัทที่กำลังจะซื้อระบบ

- ใช้ **JobTopGun** และ **JobBKK** เท่านั้น (robots อนุญาต `/jobs/`) — **JobsDB ห้าม**
- ต้องเป็น **ตารางแยกจาก tender เด็ดขาด** ประกาศรับสมัครงานไม่ใช่โอกาสยื่นข้อเสนอ
- เก็บ evidence เหมือนกัน: URL, SHA-256, เวลาที่พบ
